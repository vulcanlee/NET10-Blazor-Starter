@using System.Globalization

@code {
    protected override void OnParametersSet()
    {
        LocaleProvider.SetLocale(CultureInfo.CurrentUICulture.Name);
    }
}

<ConfigProvider>
    <Router AppAssembly="typeof(Program).Assembly" NotFoundPage="typeof(Pages.NotFound)">
        <Found Context="routeData">
            @* 最後一道網：接住並記錄所有元件內未處理的例外，避免使用者只看到空白畫面而日誌毫無痕跡。 *@
            <LoggingErrorBoundary>
                <ChildContent>
                    @*
                        AuthorizeRouteView 讓頁面上的 [Authorize] 在 circuit 內導覽時也生效（HTTP 層的閘門只管整頁請求）。
                        驗證中與未授權時一律不渲染任何內容，避免頁面先閃出來再被導走。
                    *@
                    <AuthorizeRouteView RouteData="routeData" DefaultLayout="typeof(Layout.MainLayout)">
                        <Authorizing></Authorizing>
                        <NotAuthorized>
                            <MyProject.Web.Components.Auths.RedirectToLogin />
                        </NotAuthorized>
                    </AuthorizeRouteView>
                    <FocusOnNavigate RouteData="routeData" Selector="h1" />
                </ChildContent>
                <ErrorContent>
                    <div class="alert alert-danger" role="alert">
                        <strong>這個頁面發生未預期的錯誤。</strong>
                        錯誤已記錄，請重新整理頁面；若持續發生請聯絡系統管理員。
                    </div>
                </ErrorContent>
            </LoggingErrorBoundary>
        </Found>
    </Router>

    @*
        AntDesign 的對話窗、確認窗、通知與消息條全部渲染在這個 AntContainer 底下，
        與 layout 無關。它們的全域樣式因此也必須掛在同一層 —— 掛在 layout 或各檢視裡，
        只要有頁面用了別的 layout（登入頁的 NoFooterLayout、首頁的 EmptyLayout）就會吃不到。
        ⚠️ 全專案只能在這裡渲染一次，由 FormModalConventionTests 守門。
    *@
    <OverlayStyles />

    <AntContainer />
</ConfigProvider>
