# 粉梅暖雪登入頁視覺驗證

- 驗證日期：2026/09/18
- 系統版本：0.9.23
- final result: passed
- 結論：目前沒有待修正的 P0／P1／P2 視覺問題；通過的是本機桌面瀏覽器及響應式視窗驗證，實機限制列於末節。

## 比對來源與尺寸

使用者選定第 3–4 張粉梅暖雪 Mockup。來源位於 `C:/Users/vulca/.codex/generated_images/01a0b1f4-5afc-7251-89fd-384fe11cd2e4/`：

- 桌面：`exec-5975f734-151e-4db2-a8cf-7270395f51ba.png`，1774 × 887 像素。
- 手機：`exec-ea8ea336-b0a4-436b-8f01-fbd770b3738d.png`，853 × 1844 像素。

實作使用 Codex 內建瀏覽器，CSS 視窗分別為 1800 × 900 與 390 × 844，裝置像素比約 1。原稿是高解析度概念圖；依 2:1／約 390:844 的相同比例比較構圖，並以 DOM 實際 CSS 像素驗證元件，不把來源圖片像素當成 CSS 尺寸。桌面與手機來源、實作圖在同一次圖片檢視輸入中共同比對。

預覽：`http://localhost:5119/Auths/Login`，使用獨立測試資料庫。畫面為未登入、空白表單、Google 登入關閉；驗證碼由系統產生，因此不同於稿上的固定 6134。

所有實作截圖位於 `C:/Users/vulca/AppData/Local/Temp/winter-login-qa/`：

| 檔案 | 用途 |
|---|---|
| `desktop-1800-final.png` | 桌面最終全畫面，1800 × 900 |
| `mobile-390-final.png` | 手機最終可視區，390 × 844，超出內容可垂直捲動 |
| `form-detail.png` | 桌面表單細節，520 × 638，檢查字級、邊框、欄位、驗證碼與按鈕 |
| `mobile-320.png` | 第一輪 320px 最窄版型，驗證碼上下排列 |
| `tablet-768.png` | 第一輪 768px 平板單欄 |
| `desktop-1024.png` | 第一輪 1024px 桌面雙欄 |
| `google-320-scrolled.png` | 最終版 320 × 568，Google 登入開啟並捲至下方 |

## 五項視覺檢查

- 字體與文字：沿用專案繁體中文字型堆疊。主要標題深梅色、粗體；輸入欄位實際 computed font-size 為 16px，高度 52px。系統名稱與說明仍從設定取得，未改為圖片文字。
- 間距與配置：桌面維持品牌左、表單右；面板依核定計畫限制為 520px，較概念稿窄屬刻意調整。手機縮減頂部留白，品牌後接單欄表單；不把圖稿等比例縮小。
- 顏色與材質：使用粉梅、奶白積雪、莓粉按鈕、半透明淡粉面板；加上內外亮邊、內陰影及毛玻璃，文字保持深色可辨識。真實 CSS 折光較生成稿節制，屬可接受的材質差異。
- 影像：桌面與手機使用各自生成的無 UI 背景，保留覆雪梅枝與至少三層雪丘。雪粒素材具真實 alpha。Logo 沿用原始素材，沒有用繪製形狀代替背景。
- 文案與操作：帳號、密碼、驗證碼、記住我、登入與條件式 Google 登入均為真實表單元件。移除英文裝飾標語；驗證碼提示改為不限定左右方向，符合 320px 上下排列。

## 比對與修正歷程

1. 第一輪發現手機品牌區落在主要梅枝上，列 P2；頂部間距由 80px 調為 128px，讓品牌移到枝條下方。並補上明確品牌文字顏色，避免全域標題色影響。
2. 第一輪面板邊緣過於平淡，列 P2；增加不攔截輸入的內側亮邊，保留半透明及柔和陰影。
3. 最終重新建置、重啟預覽，拍攝 `desktop-1800-final.png`、`mobile-390-final.png` 與表單細節。與來源共同檢視後，兩項問題皆已修正，沒有待修正的 P0／P1／P2。

## 功能、動態與響應式驗證

- 實測 320、390、768、1024、1440px，捲動容器沒有水平溢出。768px 為單欄、1024px 起雙欄；320px 驗證碼改為上下排列。
- 390px 顯示手機背景，遠層雪花為 `display: none`；桌面雪花為 18／28 秒，讀取 transform 可確認正在位移。
- 帳號聚焦有可見 outline 與上移效果；Tab 切至密碼、Shift+Tab 回帳號，均符合正常順序。
- 以瀏覽器操作勾選記住我、按鈕提交及 Enter 提交；空白資料顯示對應錯誤，錯誤元件帶 `role="alert"` 與淡入動畫。
- Google 開／關兩種設定皆實測。開啟使用測試用假設定，只驗證 UI 與 ReturnUrl 連結，不呼叫 Google。320 × 568 捲動後 Google 按鈕完整位於可視區，無裁切。
- 主控台檢查沒有 error／warn；背景及 Logo 均成功載入。
- 七個 HTTP 表單情境全部通過：空白帳號、空白密碼、空白驗證碼、錯誤驗證碼、錯誤帳密、成功登入與 ReturnUrl、記住我持久 Cookie。採獨立測試帳號、測試資料庫與合成表單資料，不操作使用者資料。
- 23 個既有登入／認證測試通過；最終建置 0 警告、0 錯誤。格式唯讀檢查、文件 BOM 檢查及 diff 空白檢查通過。

## 已知驗證限制與後續微調

- 此次使用桌面瀏覽器縮放視窗，沒有實體手機；觸控與 iOS／Android 軟鍵盤仍需實機驗收。已透過短視窗與垂直捲動檢查內容可達性。
- `prefers-reduced-motion` 規則已靜態檢查，會隱藏雪花、停用入場／勾選／錯誤動畫、取消位移及 transition。此瀏覽器未提供動態偏好模擬介面，因此沒有實際切換系統設定，也未宣稱完成該項動態實測。
- 無 `backdrop-filter` 的降級底色已靜態檢查，未在不支援此屬性的瀏覽器實測。
- P3：圖稿的折射高光屬生成效果，實作採 CSS 光邊與陰影近似；可依使用者實際觀感再微調。

## 交付

畫面與素材已納入原專案，保留伺服器登入流程；相關文件與版本已更新至 0.9.23，沒有自動建立 commit。
