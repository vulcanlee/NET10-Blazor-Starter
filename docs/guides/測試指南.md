# 測試指南

- 文件版本：1.9
- 文件狀態：已實作
- 現行系統版本：0.9.52
- 首次實作版本：0.2.8
- 最後核對日期：2026/09/23

本專案以 **xUnit** 撰寫單元與整合測試，測試專案為 `src/MyProject/MyProject.Tests`，並納入 CI 自動執行（見 [CI-CD 與品質檢查](../operations/CI-CD與品質檢查.md)）。

---

## 1. 測試框架與套件

`MyProject.Tests` 使用：

- **xUnit** — 測試框架（`[Fact]` / `[Theory]`）。
- **Microsoft.AspNetCore.Mvc.Testing** — 以 `WebApplicationFactory` 啟動整個 Web API 做整合測試。
- **Microsoft.NET.Test.Sdk** — 測試執行器。
- **coverlet.collector** — 程式碼覆蓋率蒐集。

實際版本以 `src/MyProject/MyProject.Tests/MyProject.Tests.csproj` 為準。

---

## 2. 測試類別與涵蓋範圍

| 測試類別 | 類型 | 涵蓋重點 |
|----------|------|----------|
| `ApiIntegrationTests` | 整合 | 端到端驗證 API：無 Bearer 回 401、login/refresh/me、`/api/v1` 版本化合約、validation 400、403、500、`/health/live`、`/health/ready`、`system-health` 未登入不外洩、Production 安全檢查 fail-fast。 |
| `ApiResultTests` | 單元 | `ApiResult<T>` 信封：驗證錯誤保留結構化 errors、伺服器錯誤包含例外資訊。 |
| `JwtTokenServiceTests` | 單元 | JWT：產生 access/refresh token、驗證 refresh token 取回使用者、拒絕以 access token 當 refresh。 |
| `MyUserServicePasswordTests` | 單元 | 變更密碼：正確舊密碼才更新、錯誤舊密碼／空白／確認不符不更新、support 帳號不可改。 |
| `AuthenticationStateHelperTests` | 單元 | `AuthenticationStateHelper.Check`：未登入、Sid 無效、使用者不存在／停用、缺 RoleView、角色 JSON 無效、強制改密碼例外頁、快取仍重檢、合法使用者初始化 `CurrentUser`。另含 `CheckAccessPage` 的上下界（0.9.37）：只有 `頁面:view` 應通過、只有 `頁面:create` 應被拒。 |
| `CacheServiceTests` | 單元 | `ICacheService`：複雜物件序列化往返、Miss 回預設值、`GetOrCreateAsync` factory 只跑一次、`RemoveAsync` 失效。 |
| `CacheSettingsTests` | 單元 | `CacheSettings`：空白預設 Memory、大小寫不敏感解析 Redis、未知值丟例外。 |
| `SystemHealthTests` | 單元 | 健康分數：全綠 100、退化黃燈、不健康紅燈、燈號對應、`HealthLogReader` 讀最後 100 行、檔案不存在回退。 |

### 2.1 完整測試涵蓋矩陣（機制對照）

上表為重點類別詳解；下表為 `MyProject.Tests` 全部測試檔對應到的機制與能力（PRD），供快速定位。

| 測試檔 | 機制 | 對應能力／文件 |
|--------|------|----------------|
| `ApiIntegrationTests` | API 端到端：授權、`/api/v1` 版本化、403/500、health 探針 | [Web API 端點目錄](../architecture/Web%20API%20端點目錄.md) |
| `ApiResultTests` | `ApiResult<T>` 信封 | [Web API 設計慣例](../architecture/Web%20API%20設計慣例.md) |
| `JwtTokenServiceTests` | JWT 產生／驗證／refresh | [登入與帳號流程](../prd/登入與帳號流程-prd.md) |
| `MyUserServiceLoginTests` | 登入流程 + 帳號鎖定（`AccessFailedCount`／`LockoutEndUtc`）| [登入與帳號流程](../prd/登入與帳號流程-prd.md) |
| `MyUserServicePasswordTests` | 變更密碼與登入自動升級 | [登入與帳號流程](../prd/登入與帳號流程-prd.md) |
| `SecurePasswordHasherTests` | PBKDF2 密碼雜湊 | [密碼種類與儲存機制](../security/密碼種類與儲存機制.md) |
| `TotpServiceTests` | TOTP（RFC 6238）二階段驗證骨架 | [登入與帳號流程](../prd/登入與帳號流程-prd.md) |
| `AuthenticationStateHelperTests` | 登入狀態、`IsAdmin`、`RoleView` 檢查 | [認證授權與權限機制](../security/認證授權與權限機制.md) |
| `PermissionCheckerTests` | 動作級授權判定（`IPermissionChecker`）| [紀錄分類與團隊權控](../prd/紀錄分類與團隊權控-prd.md) |
| `RbacWriteServiceTests` | 角色／使用者權限雙寫 | [角色管理](../prd/角色管理-prd.md) |
| `RbacBackfillServiceTests` | 啟動時 RBAC 回填 | [角色管理](../prd/角色管理-prd.md) |
| `MyUserServiceAssignmentTests` | 角色／團隊指派 | [使用者管理](../prd/使用者管理-prd.md) |
| `EffectiveTeamResolverTests` | 使用者有效團隊解析 | [紀錄分類與團隊權控](../prd/紀錄分類與團隊權控-prd.md) |
| `MenuIconTests` | 宣告式選單圖示允許清單 | [首頁與導覽](../prd/首頁與導覽-prd.md) |
| `AuditLogServiceTests` | 稽核寫入（`AuditLog`）| [認證授權與權限機制](../security/認證授權與權限機制.md) |
| `AuditEventsTests` | 使用者／角色／權限異動稽核事件 | [使用者管理](../prd/使用者管理-prd.md)、[角色管理](../prd/角色管理-prd.md) |
| `CategoryServiceTests` | 分類 CRUD | [分類清單](../prd/分類清單-prd.md) |
| `TeamServiceTests` | 團隊 CRUD | [團隊清單](../prd/團隊清單-prd.md) |
| `ProjectServiceTeamAccessTests` | 專案紀錄的團隊資料權控 | [專案項目](../prd/專案項目-prd.md)、[紀錄分類與團隊權控](../prd/紀錄分類與團隊權控-prd.md) |
| `TagStringHelperTests` | 分類／團隊標籤字串處理 | [紀錄分類與團隊權控](../prd/紀錄分類與團隊權控-prd.md) |
| `CacheServiceTests` | `ICacheService` 快取行為 | [分散式快取機制](../features/分散式快取機制.md) |
| `CacheSettingsTests` | 快取設定解析 | [分散式快取機制](../features/分散式快取機制.md) |
| `SystemHealthTests` | 健康分數與燈號 | [系統健康監控](../prd/系統健康監控-prd.md) |
| `CategoryServiceTeamVisibilityTests` | 分類依團隊過濾（0.4.40） | [分類清單](../prd/分類清單-prd.md) |
| `CategoryTeamRepositoryUniquenessTests` | API 路徑的名稱唯一性判定（0.4.41） | [分類清單](../prd/分類清單-prd.md)、[團隊清單](../prd/團隊清單-prd.md) |
| `CategoryTeamUniqueIndexMigrationTests` | 唯一索引 migration 的資料清理（0.4.41） | [分類清單](../prd/分類清單-prd.md) |
| `LogQueryServiceTests` | 日誌查詢與解析 | [日誌檢視](../prd/日誌檢視-prd.md) |
| `LogLevelRuntimeStateTests` | 執行期日誌等級調整 | [日誌等級設定](../prd/日誌等級設定-prd.md) |
| `AiSettingsTests` | AI 設定的 provider 解析、預設值，以及**守門**：`appsettings.json` 的 `AiSettings` 不得有對不到 POCO 屬性的鍵（0.9.4）| [AI 日誌分析](../features/AI日誌分析.md) |
| `AiChatEndpointTests` | Azure OpenAI／OpenAI 的位址、認證與設定驗證（0.9.4）| [AI 日誌分析](../features/AI日誌分析.md) |
| `AiLogPromptBuilderTests` | 提示詞組裝；**守門**：0.9.7 移除所有字元上限後，一筆 50000 字元的日誌要完整送出（`Build_ShouldSendEveryCharacter_WhenWithinEntryLimit`）| [AI 日誌分析](../features/AI日誌分析.md) |
| `AiChatResponseParserTests` | 回應與用量解析，含 Azure 內容過濾的形狀差異（0.9.4）| [AI 日誌分析](../features/AI日誌分析.md) |
| `AiMarkdownRendererTests` | **安全守門**：AI 輸出的 HTML 轉義、連結 scheme 白名單、移除圖片（0.9.4）| [AI 日誌分析 §6](../features/AI日誌分析.md) |
| `AiLogAnalysisServiceTests` | HTTP 呼叫、錯誤對應、**不重試**與**錯誤訊息不含金鑰**（0.9.4）| [AI 日誌分析](../features/AI日誌分析.md) |
| `TokenUsageLogServiceTests` | LLM 用量記錄與查詢：寫檔＋寫列、失敗也記、分組統計、**合計不重複計算快取與推理**、費用快照寫入（0.9.14／0.9.17）；`GetDailyAsync` 逐日統計 —— 依當地日期分組（⭐ 同時驗證 `GroupBy(x => x.OccurredAt.Date)` 在 SQLite **譯得出 SQL**）、逐日費用與未定價計數、套用篩選、依日期遞增（0.9.50）| [Token 用量](../prd/Token用量-prd.md) |
| `TokenUsageRangesTests` | 「最近 N 天」＝**含今天往前數 N 日**：近 1 天為今天、近 7／30 天的起日與天數、傳入帶時分秒也要得到乾淨日期。釘住這個定義，摘要卡標題與實際查詢區間才不會各說各話（0.9.50）| [Token 用量 §3.2.1](../prd/Token用量-prd.md) |
| `TokenUsageReportPdfBuilderTests` | 用量 PDF 報表：簽章、明細上限、依時長計費與全未定價都排得出來（0.9.14／0.9.17）；每日趨勢區塊、六種匯出範圍、⭐ **單頁籤仍含條件區**、⭐ **全部費用為 0 時不可除以零**（0.9.51）；⭐ **長中文篩選值真的有換行**（0.9.52）| [Token 用量](../prd/Token用量-prd.md) |
| `AiUsageCostCalculatorTests` | ⭐ 費用計算：**子集晶格不重複計費**、**受限前綴比對拒絕 `gpt-4o-mini`**、長脈絡門檻、四種計費單位、算不出來回 `null`（0.9.17）| [日誌與設定檔說明 §4.9](../operations/日誌與設定檔說明.md) |
| `AiPricingSettingsTests` | ⭐ **遞迴**未知鍵守門（打錯費率鍵名會靜默變 0）＋範本費率數字釘住（0.9.17）| [日誌與設定檔說明 §4.9](../operations/日誌與設定檔說明.md) |
| `ExportEncodingConventionTests` | ⭐ 匯出檔 UTF-8 BOM 守門：`MyProject.Web` 除 `TextDownloadPayload` 外不得自接編碼旗標（0.9.16）| [開發慣例與限制速查 §8](../architecture/開發慣例與限制速查.md) |
| `AiReportPdfBuilderTests` | PDF 產生與**內嵌字型存在性**（0.9.4）；⭐ **長中文段落真的有換行**（比較長短兩份的頁數，0.9.52）| [AI 日誌分析 §7](../features/AI日誌分析.md) |
| `CjkLineBreakTests` | ⭐ 中文斷行規則：行首禁則（句讀不落行首）、行尾禁則（開括號不留行尾）、全形空格後要斷得開、不動英文單字；⭐⭐ **切段後接回去必須與原文一字不差**（這條就是「不新增任何字元」的保證，擋掉插入零寬空格那條會印出方框的路）（0.9.52）| [速查表 §6.11](../architecture/開發慣例與限制速查.md) |
| `DatabaseUsageServiceTests` | 資料庫用量統計與估算 | [資料庫用量](../prd/資料庫用量-prd.md) |
| `UploadFileTypePolicyTests` | 上傳副檔名白名單與 ContentType（0.4.35） | [檔案上傳機制](../features/檔案上傳機制.md) |
| `RateLimitPartitionTests` | 限流依呼叫端分割（0.4.35 / 0.4.37） | [CI-CD 與品質檢查](../operations/CI-CD與品質檢查.md) |
| `MenuPermissionConsistencyTests` | ⭐ 選單／權限鍵四方一致性守門（0.4.33） | [首頁與導覽](../prd/首頁與導覽-prd.md) |
| `AdminOnlyPermissionTests` | ⭐ 管理員專屬權限鍵不得上架角色矩陣（0.4.33）；`export` 不得回到矩陣（0.9.37） | [首頁與導覽](../prd/首頁與導覽-prd.md) |
| `SidebarLinkColorConventionTests` | ⭐ 側邊欄葉節點的 `<a>` 必須有顏色覆寫且帶 `!important`（0.9.40）| [介面視覺設計規範](../architecture/介面視覺設計規範.md) |
| `DataAccessServiceLifetimeTests` | ⭐ 資料服務必須注入 `IDbContextFactory`（0.4.36） | [開發慣例與限制速查](../architecture/開發慣例與限制速查.md) |
| `LoggingConventionTests` | ⭐ 日誌四類慣例與敏感資訊禁令 | [日誌與設定檔說明](../operations/日誌與設定檔說明.md) |
| `ButtonIconConventionTests` | ⭐ 按鈕圖示慣例（不得用 emoji，0.4.27） | [開發慣例與限制速查](../architecture/開發慣例與限制速查.md) |
| `MenuIconTests` | ⭐ `Menu.json` 圖示有效性 | [首頁與導覽](../prd/首頁與導覽-prd.md) |
| `FormModalConventionTests` | ⭐ 表單對話窗六項骨架要求、果凍寫在共用基底（0.9.25–0.9.28）| [對話窗 UI 設計規範](../architecture/對話窗%20UI%20設計規範.md) |
| `ModalKeyboardConventionTests` | ⭐ 表單層不得攔截鍵盤事件（0.9.3）| [速查表 §6.3](../architecture/開發慣例與限制速查.md) |
| `AiModalStyleConventionTests` | ⭐ AI 對話窗字級必須跟著三顆按鈕縮放（0.9.8）| [AI 日誌分析](../features/AI日誌分析.md) |
| `LogoutConfirmationConventionTests` | ⭐ 三個登出入口一律走 `LogoutConfirm`（0.9.29）| [對話窗 UI 設計規範](../architecture/對話窗%20UI%20設計規範.md) |
| `ThemeConventionTests` | ⭐ 色票只能住在 `theme.css`、載入順序、**清單自己不得過期**（0.9.30–0.9.34）| [介面視覺設計規範](../architecture/介面視覺設計規範.md) |
| `DocumentationConventionTests` | ⭐ appsettings 區段兩份文件都要有、nlog layout 與 `/logs` 解析器欄位數一致（0.9.35）| [腳手架開發指引](腳手架開發指引.md) |

> ⭐ 標記者為**慣例守門測試**：它們不驗證業務行為，而是防止既有慣例被無聲地破壞。
> 改動相關機制時如果它們變紅，通常代表改法本身有問題，而不是測試該改。

> 表格以測試檔命名對應機制；實際涵蓋情境以各測試檔內容為準。新增機制時應同步補測試並更新本矩陣。

### 2.2 ⚠️ 目前最大的缺口：零個元件測試

全部約 602 個測試裡，**沒有一個渲染 Blazor 元件**。
所有 UI 的保證都是「用正規表示式掃原始碼」（上面那些 ⭐ 守門測試）。

這代表：**你可以把畫面改壞，而 CI 全綠。**
0.9.30 那次全站視覺改版（色票、側邊欄、表格、對話窗）就是這樣通過 CI 的 ——
實際的視覺驗收只能靠人眼。

也沒有端對端測試。repo 根目錄有 `.playwright-cli/` 目錄，但沒有接進 CI。

補強方式（bUnit 與 Playwright 的成本評估）見
[腳手架開發指引 §10.1](腳手架開發指引.md)。

---

## 3. 在本機執行

```powershell
# 執行整個方案的所有測試
dotnet test src/MyProject/MyProject.slnx

# 只跑測試專案
dotnet test src/MyProject/MyProject.Tests/MyProject.Tests.csproj

# 以單一測試類別或方法過濾
dotnet test src/MyProject/MyProject.slnx --filter "FullyQualifiedName~ApiIntegrationTests"
dotnet test src/MyProject/MyProject.slnx --filter "Name=GetProvider_ShouldThrow_WhenUnknown"
```

CI 以 Release 組態執行：`dotnet test src/MyProject/MyProject.slnx --configuration Release --no-build`（先經 restore/build 階段）。

---

## 4. 覆蓋率

`coverlet.collector` 已隨測試專案引入，可於執行時蒐集覆蓋率：

```powershell
dotnet test src/MyProject/MyProject.slnx --collect:"XPlat Code Coverage"
```

報表（Cobertura XML）會輸出至各測試專案的 `TestResults/` 下，可再以報表工具轉檢視。

---

## 5. 撰寫新測試的慣例

- 整合測試走 `WebApplicationFactory`，驗證對外 **`ApiResult` 合約**（狀態碼 + 信封結構），不依賴內部實作細節；需觸發 403／500 等情境時，透過測試專案的 probe controller 製造，**不新增正式 API contract**。
- 單元測試針對 Service / Helper 的純邏輯，必要時以 in-memory 或測試替身隔離資料庫。
- 新增 / 修改 API 時，請補對應整合測試；新增 Entity 或密碼／權限邏輯時，補單元測試。對照 [維護規範](../operations/維護規範.md) 的 commit 前檢查清單。

---

## 6. 延伸閱讀

- [CI-CD 與品質檢查](../operations/CI-CD與品質檢查.md) — CI 如何執行測試與其他品質關卡。
- [Web API 設計慣例](../architecture/Web%20API%20設計慣例.md) — `ApiResult<T>` / `PagedResult<T>` 合約。
- [系統健康監控](../features/系統健康監控.md) — 健康分數與燈號邏輯。
