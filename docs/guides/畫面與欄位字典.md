# 畫面與欄位字典

- 文件版本：1.9
- 文件狀態：已實作
- 現行系統版本：0.9.60
- 首次實作版本：0.9.33
- 最後核對日期：2026/09/24

## 目的

把系統的**每一個畫面、每一個欄位**講清楚：這一頁在做什麼、這個欄位是什麼意思、什麼時候要用它。

### 與 PRD 的分工

| 文件 | 回答的問題 |
|------|-----------|
| [`docs/prd/`](../prd/README.md) 各份 PRD | 「這一頁**怎麼運作**」—— 能力範圍、流程、權限、驗收條件 |
| **本文件** | 「這個**欄位是什麼**」—— 意思、用途、限制、容易誤解的地方 |

兩者都列欄位名，但角度不同。要改功能看 PRD，要搞懂某一欄看這裡。

### 怎麼讀

每個畫面固定三段：

1. **一段白話** —— 這頁做什麼、誰會用。不出現程式屬性名，給操作者看。
2. **這一頁可以做什麼** —— 工具列與動作。
3. **欄位對照** —— 給開發者的表：畫面上的名稱 ↔ 程式屬性 ↔ 限制 ↔ 意義 ↔ 陷阱。

⚠️ 標記代表「這一欄的字面意思會誤導你」，通常是本文件最值得讀的部分。

---

## 〇、先讀這段：全站共通的概念

這五組概念跨越多個畫面，先在這裡講一次，各畫面不再重複。

### 0.1 「團隊」在不同畫面語意不同 ⚠️

同樣叫「團隊」，**留空的意思完全不一樣**，這是本系統最容易搞錯的地方：

| 畫面 | 欄位 | 留空的意思 |
|------|------|-----------|
| 專案項目 | 團隊 | **公開給所有人看得到**（不是「沒人能看」）|
| 分類清單 | 適用團隊 | **所有團隊都能使用這個分類** |
| 角色管理 | 預設團隊 | 這個角色**不帶來任何團隊**；若使用者也沒有直接綁團隊，就**只看得到沒有團隊的公開紀錄** |
| 使用者管理 | 團隊 | **不是「沒有團隊」**：可見範圍一律是「直接綁定的團隊 ＋ 所有角色（主要＋額外）的預設團隊」，留空只是少了直接綁定那一份 |

前兩個是「留空＝最寬鬆」，第三個是「留空＝最嚴格」。畫面上的 placeholder 都寫了，但很少人會讀。

⚠️ 「只看得到公開紀錄」只對**專案**成立。分類剛好相反：可見團隊是空的使用者，分類清單與下拉選單看得到**全部**分類。

> 為什麼分類與紀錄的規則相反：分類的可見性只是下拉選單的便利性過濾，
> 真正的安全邊界由 RBAC 負責，所以刻意放寬。見[速查表 §4.1](../architecture/開發慣例與限制速查.md)。

### 0.2 團隊用「名稱」比對，不是「代號」⚠️

`Project.Teams`、`Category.Teams` 以及角色的「預設團隊」存的都是**團隊名稱字串**，不是 `Team.Code`；
只有使用者的直接綁定（使用者管理的「團隊」）是存團隊 Id。

- **改團隊名稱**：使用者的直接綁定會跟著新名稱走，但專案、分類、角色預設團隊上的**舊名稱不會更新** ——
  可見性會靜默出錯。改代號則沒有影響，代號目前只是給人看的備註。
- **停用團隊**：只會從下拉選單消失，**仍然計入**使用者的可見範圍，不能當成「收回權限」使用。
- **刪除團隊**：使用者的直接綁定會自動解除，但紀錄上的名稱字串不會清掉。

### 0.3 權限有三層，不是一層

| 層 | 欄位 | 行為 |
|----|------|------|
| 管理員 | 使用者的「管理員」勾選 | **短路所有動作級檢查**，也不受團隊過濾。不是一種角色 |
| 角色 | 主要角色 ＋ 額外角色 | 權限取**聯集**；但清單的「角色」欄只顯示主要角色 |
| 團隊 | 使用者團隊 ∪ 角色預設團隊 | 決定看得到哪些**資料列**，與能做哪些**動作**是兩回事 |

### 0.4 時間欄位

所有清單的「建立時間／更新時間」都是系統自動寫入，**沒有任何畫面可以修改它們**。
顯示為當地時間。

### 0.5 側邊選單位置

| 選單 | 畫面 | 本文 |
|------|------|------|
| 首頁 | `/App` | 2.2 |
| 專案管理 › 專案項目 | `/projects` | 3.1 |
| 資料定義 › 分類清單／團隊清單 | `/categories`／`/teams` | 3.2／3.3 |
| 系統管理 › 統計與分析 › 系統健康監控／日誌檢視／日誌等級設定／系統例外紀錄／資料庫用量／Token 用量 | `/system-health`／`/logs`／`/log-level-setting`／`/system-exceptions`／`/database-usage`／`/token-usage` | 5.1～5.6 |
| 系統管理 › 權限管理 › 使用者管理／角色管理／稽核紀錄 | `/myusers`／`/roleviews`／`/audit-logs` | 4.1～4.3 |
| 登出 | `/auths/logout` | 1.3 |

選單會依權限過濾，看不到的項目就是沒有權限（見 6.4）。

---

## 一、登入與帳號

### 1.1 登入　`/Auths/Login`

沒登入的人會被導到這裡。輸入帳號密碼與驗證碼即可進入系統；若系統有開 Google 登入，
也可以直接用 Google 帳號進來。

**這一頁可以做什麼**：登入（按鈕「登入系統」）、勾選記住我、用 Google 登入（需設定才會出現）、
忘記密碼（「記住我」右側的連結，只有啟用寄信時出現，見 1.6；沒啟用時那個位置顯示「企業級安全登入」字樣）。
從重設密碼頁成功導回時，表單上方會出現綠色的「密碼已重設，請以新密碼登入。」。

| 畫面上的名稱 | 對應屬性 | 限制 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 帳號 | `Input.Account` | 必填 | 系統帳號，不是 Email | 預設管理者帳號是 `support`，預設密碼也是 `support`（由 `BootstrapSettings` 決定，每次啟動會覆寫回設定值）|
| 密碼 | `Input.Password` | 必填 | 登入密碼，PBKDF2 雜湊後比對 | 與 1.5 的「API 密碼」是**同一組密碼**（同一個欄位）|
| 驗證碼 | `Input.CaptchaInput` | 必填，4 位數，前後空白會去掉 | 防自動化嘗試 | 登入失敗會**重新產生**，不能沿用上一組 |
| 記住我 | `Input.RememberMe` | — | 勾選後關掉瀏覽器仍維持登入 | 原理見[記住我登入原理說明](../security/記住我登入原理說明.md) |
| 使用 Google 登入 | — | 條件顯示 | 只有在 `GoogleOAuthSettings` 設定完成時才出現 | 第一次用 Google 登入會建立**停用中**的帳號並導向 1.2 |

### 1.2 帳號待審核　`/Auths/Pending`

用 Google 第一次登入時，系統會幫你建一個帳號，但**預設是停用的**，要等管理員啟用。
這一頁就是告訴你「已經建好了，請等通知」。純說明頁，沒有欄位。

### 1.3 登出　`/Auths/Logout`

沒有畫面。點登出會先跳確認窗（0.9.29 起），確認後才真的登出。

> 系統自己發動的強制登出（session 失效、帳號被停用、角色被移除）**刻意不跳確認** ——
> 那是系統行為不是你的意圖，對已經失效的 session 問「確定要登出嗎」只會讓人卡住。

### 1.4 變更密碼　`/ChangePassword`（也可從右上角使用者選單開對話窗）

改自己的登入密碼。要先輸入目前密碼才能改。

| 畫面上的名稱 | 對應屬性 | 限制 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 目前密碼 | `CurrentPassword` | 必填 | 驗證是本人 | 忘了目前密碼：啟用寄信的系統可用 1.6「忘記密碼」自助重設；沒啟用寄信只能請管理員改 |
| 新密碼 | `NewPassword` | 必填 | 新的登入密碼 | |
| 確認新密碼 | `ConfirmPassword` | 必填、需與新密碼相同 | 防打錯 | |

> **頁面與對話窗的檢查不完全一樣**：
>
> | | 對話窗（右上角選單）| `/ChangePassword` 頁面 |
> |---|---|---|
> | 送出鈕 | 確認變更 | 儲存（另有「取消」回首頁）|
> | 新密碼長度 | **至少 6 個字元** | **沒有下限** |
> | 目前密碼打錯 | 「目前密碼輸入錯誤。」 | 「目前密碼不正確。」 |

> `support` 開發帳號被擋住不能從這裡改（頁面顯示「系統預設開發帳號 support 禁止變更密碼」）——
> 它的密碼由 `BootstrapSettings` 控制，每次重啟會被設定檔覆寫回去。

### 1.5 設定 API 密碼　`/Profile`

用**帳號**＋密碼去打 `/api/v1/auth/login` 換取 JWT token 時用的密碼。

⚠️ **它和網頁登入密碼是同一組**（寫進同一個欄位），頁面上的說明文字容易讓人誤會：

- 用 Google 登入、**從來沒設過密碼**的帳號：這裡是「第一次設定本地密碼」，不用輸入目前密碼；
  設好之後，這組帳號密碼**也能用來登入網頁**。
- 一般帳號：這裡**等同變更登入密碼**，改完網頁登入也要用新密碼。
- API 登入比對的是「帳號」欄。Google 建立的帳號，帳號本身就是 Email，所以畫面上寫「帳號（Email）」。
- 頁面說明寫「此密碼僅供 API 存取使用」，與實際行為不符（見上兩點）。

`support` 帳號在這一頁會看到「系統預設開發帳號 support 禁止於此變更密碼」，沒有表單。

| 畫面上的名稱 | 對應屬性 | 限制 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 目前密碼 | `CurrentPassword` | 只在帳號已有本地密碼時出現 | 驗證是本人 | Google 帳號第一次設定時不會出現 |
| 新密碼 | `NewPassword` | 必填 | API 與網頁登入共用 | **與 1.4 是同一個欄位**，改這裡就是改登入密碼 |
| 確認新密碼 | `ConfirmPassword` | 需相同 | | |

### 1.6 忘記密碼　`/Auths/ForgotPassword`（0.9.60 起）

忘了密碼時，從登入頁的「忘記密碼？」進來。輸入帳號或 Email，系統會寄一封含重設連結的信。

⚠️ **只有系統啟用寄信（`EmailSettings:Provider` 不是 `None`）才有這個功能**。沒啟用時登入頁沒有連結，
直接開這個網址只會看到「系統目前未啟用寄信功能…請聯絡系統管理員」。

**這一頁可以做什麼**：寄出重設信（按鈕「寄出重設信」）、返回登入。

| 畫面上的名稱 | 對應屬性 | 限制 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 帳號或 Email | `Input.Identifier` | 必填 | 先當成帳號找，找不到再當成 Email 找（不分大小寫）| 同一個 Email 對到多個帳號時，**每個帳號各寄一封** |
| 驗證碼 | `Input.CaptchaInput` | 必填，4 位數 | 與登入頁同一套 | 只是減速帶，擋不了腳本；真正的防線是同帳號 60 秒內不重寄 |

送出後**一律**顯示「如果帳號存在且登記了有效的 Email，重設密碼的信已經寄出，請在 30 分鐘內點信中的連結…」。
不論帳號存不存在、有沒有真的寄出，畫面都一樣 —— 這一頁不能被拿來試探帳號。
以下情況**不會寄信**（原因只記在稽核紀錄 `Password.ResetRequested`）：

- 帳號是 `support`、帳號已停用、只用 Google 登入而沒有本地密碼、Email 空白或格式不對
- 同一帳號 60 秒內已經申請過（`PasswordResetSettings:RequestCooldownSeconds`）

### 1.7 重設密碼　`/Auths/ResetPassword?token=…`（0.9.60 起）

從重設信的連結打開。連結 30 分鐘內有效、**只能用一次**；同一帳號後來又申請了一次，舊信的連結就失效。

| 畫面上的名稱 | 對應屬性 | 限制 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 副標題 | `account` | — | 「為帳號「X」設定新密碼（至少 6 個字元）」，讓人確認重設的是哪個帳號 | 一個 Email 有多個帳號時，看這裡分辨是哪一封 |
| 新密碼 | `Input.NewPassword` | 至少 6 個字元，不可為 `123456` | 新的登入密碼 | `123456` 是「強制變更密碼」的哨兵值，設了會被一直導去改密碼，所以擋掉 |
| 確認新密碼 | `Input.ConfirmPassword` | 需相同 | 防打錯 | |

- 密碼不符規則時顯示原因，**連結仍然有效**，改正後再送一次即可。
- 成功後導回登入頁並顯示「密碼已重設，請以新密碼登入。」；帳號若因連續打錯密碼被鎖住，會一併解鎖。
  帳號主人另外會收到一封「您的密碼已變更」通知信。
- 連結過期、已用過、被竄改，或申請之後帳號被停用：一律顯示「重設連結無效或已過期，請重新申請。」，
  並提供「重新申請重設信」連結。
- ⚠️ 重設後，**其他裝置上已經登入的工作階段不會被登出**（系統沒有 security stamp）。

---

## 二、一般畫面

### 2.1 啟動頁　`/`

進入系統的第一個畫面，顯示「系統載入中，正在為你準備工作環境...」。約 3 秒後檢查登入狀態，
已登入就自動進入首頁 `/App`。沒有欄位。

### 2.2 首頁　`/App`

登入後的落地頁，分三段：

1. **系統介紹**：系統名稱與描述。
2. **系統能力**：六張介紹卡（權限與角色控管、專案項目管理、分類與團隊定義、日誌檢視與 AI 分析、
   健康監控與資料庫用量、檔案上傳與保管），純說明、不可點。
3. **快速入口**：專案項目／分類清單／團隊清單三個連結，**依你的權限過濾**；一個都沒有權限時整段隱藏。

⚠️ 「權限與角色控管」卡片提到「匯出」動作，但角色矩陣目前只有檢視／新增／編輯／刪除（匯出已於 0.9.37 下架）。

底部顯示系統版本與執行環境（Development／Production），回報問題時把這兩個一起附上。

---

## 三、業務資料

### 3.1 專案項目　`/projects`

系統內建的示範業務模組，也是你之後新增自己模組時的樣板。
每個專案可以掛分類標籤、團隊標籤，還可以上傳附件。

瀏覽器分頁標題為「專案管理」。

**這一頁可以做什麼**：新增、修改、刪除、重新整理、關鍵字搜尋（有輸入時多一個「清空搜尋」）、
依分類過濾、依團隊過濾（兩者皆可複選）。新增／修改／刪除按鈕會依你的權限個別顯示或隱藏。
清單每頁 8 筆。

- 關鍵字只比對標題、描述、狀態、優先級、負責人，**不比對分類與團隊**（那兩個用過濾器）。
- 團隊過濾器與編輯窗的團隊選項列出**所有啟用中的團隊**，不只你所屬的；但清單只會出現你看得到的紀錄。

**新增時的預設值**：狀態「未開始」、優先級「中」、完成百分比 0，
**團隊預先帶入你的有效團隊中仍啟用的那些**（0.9.57 起；已停用的團隊會略過）。
預設值不算你的修改 —— 開窗後直接取消不會詢問「是否放棄變更」。
沒有任何有效團隊的人（例如沒綁團隊的管理員）團隊欄位維持空白。

**清單欄位**

| 畫面上的名稱 | 對應屬性 | 型別／限制 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 標題 | `Title` | 必填 | 專案名稱，預設遞增排序 | |
| 描述 | `Description` | 選填 | 補充說明 | |
| 開始日期 | `StartDate` | 選填 | 預計或實際開始日 | |
| 結束日期 | `EndDate` | 選填 | 預計或實際結束日 | **不可早於開始日期**，儲存時會擋 |
| 狀態 | `Status` | 五選一 | 未開始／進行中／已完成／暫緩／等待 | 0.9.30 起顯示為徽章（已完成綠、進行中強調色、暫緩橘、未開始與等待灰）；合法值由 `StatusOptions` 界定、服務層把關 |
| 優先級 | `Priority` | 三選一 | 低／中／高，預設「中」 | |
| 完成百分比 | `CompletionPercentage` | 0–100 整數 | 進度 | 輸入框限制 0～100；超出範圍儲存時會擋（「完成百分比 必須介於 0 到 100」）|
| 負責人 | `Owner` | 必填 | **純文字姓名，不是系統帳號** | 不會與使用者管理連動，打錯不會有人提醒 |
| 分類 | `CategoriesText` | 顯示用 | 多個分類以「、」串起來 | 這是投影欄位；實際值在 `Categories` 清單 |
| 團隊 | `TeamsText` | 顯示用 | 同上 | **留空＝公開給所有人**（見 §0.1）|
| 建立時間 | `CreatedAt` | 自動 | | |
| 更新時間 | `UpdatedAt` | 自動 | | |
| 操作 | — | — | 修改／刪除 | 這一欄固定在右側，捲動時不會跑掉 |

**編輯表單**（分四個區塊）

| 區塊 | 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 基本資料 | 標題 / 負責人 / 描述 | `Title` / `Owner` / `Description` | 同上 | 描述獨占整行 |
| 排程與進度 | 開始日期 / 結束日期 / 狀態 / 優先級 / 完成百分比 | 同上 | 同上 | |
| 分類與團隊 | 分類 | `Categories` | 可複選；下拉**只列出你所屬團隊可用的分類** | 若紀錄原本掛了你看不到的分類，系統會額外插入一個「（已限定其他團隊）」選項，避免存檔時被無聲移除 |
| | 團隊 | `Teams` | 可複選，決定誰看得到這筆；新增時預帶你的有效團隊 | 留空時儲存前會跳提醒「此專案未指定團隊，將對所有使用者公開可見。確定要這樣儲存嗎？」|
| 專案附件 | 新增附件 | — | 可一次選多檔，**每檔上限 1GB** | 超過時選檔當下就擋（「{檔名} 超過 1GB 限制」）；副檔名有白名單，`.html`／`.svg`／`.exe` 等預設擋掉 |
| | 待上傳檔案 | — | 這次選了、但還沒按儲存的檔 | **取消編輯會一起丟掉**；未儲存提示有把它算進去 |
| | 已上傳檔案 | `Files` | 先前存好的檔，可下載或按「移除」標記刪除 | 刪除紀錄時實體檔也會被刪 |

必填：標題、負責人、狀態、優先級、完成百分比。**儲存流程**：表單檢查 → 修改時若沒有任何變更，
直接提示「沒有任何變更，未進行儲存。」→ 儲存確認窗 → 團隊留空時再跳公開提醒。

### 3.2 分類清單　`/categories`

維護「分類」這個主檔。分類會出現在專案項目的分類下拉選單裡。

**這一頁可以做什麼**：新增、修改、刪除（依「分類清單」權限個別顯示）、重新整理、搜尋（比對名稱與描述）。

⚠️ **這份清單本身也依團隊過濾**，規則與下拉選單相同（見 §0.1）：你看不到只限其他團隊使用的分類。

**儲存前的提醒**（先跳儲存確認窗，再視情況跳其中一個）：

- 適用團隊留空：「此分類未指定適用團隊，將出現在所有使用者的分類下拉清單中（視同公用分類）。確定要這樣儲存嗎？」
- 非管理員、且指定的團隊與你所屬的團隊沒有交集：「此分類指定的團隊不包含你所屬的任何團隊，
  儲存後你將無法在分類清單中看到這筆資料。確定要這樣儲存嗎？」

| 畫面上的名稱 | 對應屬性 | 型別／限制 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 名稱 | `Name` | 必填，最長 100，**全系統唯一** | 分類名稱 | 比對前會去除前後空白且不分大小寫；資料庫有唯一索引兜底 |
| 描述 | `Description` | 最長 2000 | 說明這個分類用在哪 | |
| 適用團隊 | `TeamsText`／`Teams` | 多值 | 哪些團隊可以使用這個分類 | **留空＝所有團隊都能用**（與專案的團隊語意相反，見 §0.1）|
| 啟用狀態 | `IsEnabled` | 預設啟用，表單為開關（啟用／停用）| 停用後不再出現在專案的下拉選單 | 清單顯示為徽章；停用**不會**影響已經掛了這個分類的既有專案 |
| 更新時間 | `UpdatedAt` | 自動 | | 預設遞減排序 |

### 3.3 團隊清單　`/teams`

維護「團隊」主檔。團隊是本系統做資料分權的單位 —— 決定誰看得到哪些紀錄。

**這一頁可以做什麼**：新增、修改、刪除（依「團隊清單」權限個別顯示）、重新整理、搜尋（比對名稱、代號、描述）。

⚠️ 刪除使用中的團隊**不會有任何警告**；改名也不會更新既有紀錄。停用、改名、刪除的後果見 §0.2。

| 畫面上的名稱 | 對應屬性 | 型別／限制 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 名稱 | `Name` | 必填，最長 100，**全系統唯一** | 團隊名稱 | **這個字串就是分權比對的鍵**（見 §0.2）。改名會影響既有紀錄的比對 |
| 代號 | `Code` | 選填，最長 50，有填則需唯一（去除前後空白、不分大小寫比對）| 對外系統整合或報表用的短代號 | **目前不參與任何分權比對**，純備註 |
| 描述 | `Description` | 最長 2000 | | |
| 啟用狀態 | `IsEnabled` | 預設啟用 | 停用後不會出現在各處的團隊下拉選單 | ⚠️ **不會收回可見性**：已綁定的人仍看得到它的資料（§0.2）|
| 更新時間 | `UpdatedAt` | 自動 | | 預設遞減排序 |

---

## 四、系統管理（僅管理員）

### 4.1 使用者管理　`/myusers`

維護系統帳號，並在同一張表單指派角色與團隊。

**這一頁可以做什麼**：新增、修改、刪除、重新整理、搜尋（比對帳號／名稱／Email／角色名稱）。
清單預設依帳號遞增排序。

**新增時的預設值**：角色預設為名稱「預設角色」的那一個（沒有就取第一個），「啟用」勾選、「管理員」不勾。

**清單欄位**

| 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|
| 帳號 | `Account` | 登入用帳號 | 唯一性檢查目前**偏弱**（未去空白、區分大小寫、無資料庫索引），已知待改善 |
| 名稱 | `Name` | 顯示用姓名 | 屬個資，**不會寫進日誌** |
| Email | `Email` | 聯絡信箱，沒有格式檢查 | 0.9.60 起是**忘記密碼寄信的收件者**：留空或格式不對，這個帳號就無法自助重設密碼（畫面不會提示）。⚠️ API 登入比對的是「帳號」不是 Email（Google 建立的帳號，帳號本身就是 Email）|
| 角色 | `RoleViewName` | **只顯示主要角色** | 額外角色不會出現在這一欄，但權限有算進去（見 §0.3）|
| 狀態 | `StatusText` | 啟用／停用 | 0.9.30 起顯示為徽章 |
| 管理員 | `IsAdminText` | 是／否 | 0.9.30 起「是」顯示徽章，「否」顯示「—」 |
| 建立時間 / 更新時間 | `CreateAt` / `UpdateAt` | 自動 | |

**編輯表單**

| 區塊 | 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 帳號與登入 | 帳號 | `Account` | 必填 | |
| | 密碼 | `Password` | 新增時必填 | **編輯時留白＝不變更**，不是「清空密碼」（欄位提示「編輯模式留白表示沿用既有密碼」）|
| 基本資料 | 名稱 / Email | `Name` / `Email` | 名稱必填 | |
| 角色與團隊 | 角色 | `RoleViewId` | 必填，單選，主要角色 | |
| | 額外角色（多角色，權限取聯集）| `AdditionalRoleIds` | 可另外指派多個角色 | 權限是**聯集**不是覆蓋；清單的「角色」欄看不到這些 |
| | 團隊（直接綁定此使用者）| `TeamNames` | 這個人可以存取哪些團隊的資料 | 實際範圍＝這裡 ＋ 所有角色的預設團隊（**加總，不是取代**，見 §0.1）。留空時儲存前會提醒：角色有預設團隊時列出它們，沒有時警告「將只能看到無團隊標記的公開紀錄」；勾選管理員則略過 |
| 狀態 | 啟用 | `Status` | 停用後無法登入，但保留歷史紀錄 | 與刪除不同：刪除會連紀錄一起消失 |
| | 管理員 | `IsAdmin` | **會短路所有動作級授權檢查** | 勾了之後團隊過濾也不生效；勾選時「團隊留空」的提醒會自動略過 |

### 4.2 角色管理　`/roleviews`

定義角色，以及每個角色能做哪些事。

**這一頁可以做什麼**：新增、修改、刪除、重新整理、搜尋。

**清單欄位**只有：名稱、建立時間、更新時間、操作。預設團隊與權限只在編輯窗裡看得到。

**編輯表單**

| 區塊 | 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|---|
| 角色資料 | 名稱 | `Name` | 必填，角色名稱 | 唯一性檢查偏弱（完全相同才算重複），同 4.1 的帳號 |
| | 預設團隊 | `DefaultTeams` | 擁有這個角色的人都會加上這些團隊 | **留空＝這個角色不帶來任何團隊**（見 §0.1）|
| 權限設定 | 權限矩陣 | `RolePermission` | 每個頁面一列：「（全部）」＋ 檢視／新增／編輯／刪除 | 勾「（全部）」等於該頁所有動作（含以後新增的動作），並會鎖住四個動作的勾選 |

權限矩陣目前列出的頁面：首頁；專案管理功能 › 專案項目；資料定義管理功能 › 分類清單、團隊清單；登出。

> **`TabViewJson` 不是畫面上的欄位。** 它是舊的權限彙總字串，由系統從權限矩陣自動產生，
> 現在只剩「角色編輯畫面回填」的用途，**不參與權限判定**（判定讀 RBAC 關聯表）。不要手動編輯它。

> **管理員專屬頁面的權限鍵刻意不出現在矩陣裡。** 使用者管理、角色管理、稽核紀錄、系統例外紀錄與統計分析各頁
> 都只靠「管理員」旗標放行，勾不到也授不了 —— 這是刻意設計，由守門測試固定。

---

### 4.3 稽核紀錄　`/audit-logs`

查「誰、何時、對什麼、做了什麼、結果如何」。事件由各業務流程自動寫入，本頁只負責看。

**這一頁可以做什麼**：查詢、重新整理（連動作下拉選項一起更新）、查看明細、匯出目前查詢結果（CSV）、
清除 365 天前的紀錄、清空全部紀錄。**不能**新增或修改，也**不能**刪除單一列。

**查詢條件**：發生時間（起）～（迄）、動作（「動作：不限」＋資料庫實際出現過的值）、結果（不限／成功／失敗）、
操作者帳號、關鍵字（比對動作、目標、摘要，也會比對操作者帳號與目標識別）。

**CSV**：0 筆時按鈕停用；最多匯出 **10,000** 筆。欄位為
`發生時間（本地）,結果,動作,操作者帳號,操作者Id,目標類型,目標識別,摘要`。

| 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|
| 發生時間 | `OccurredAt` | 事件時間，可排序，預設最新在前 | **資料庫存的是 UTC，畫面顯示伺服器本地時間**；明細窗兩個都列 |
| 結果 | `Success` | 成功綠、失敗紅 | 登入失敗、權限被拒都算「失敗」，不是系統錯誤 |
| 動作 | `Action` | 動作代碼，例如 `Login.Success` | 以顏色區分類別：Login 藍、User 綠、Role 紫、Permission 紅、Audit 火山橘、Email 青、Password 橘（忘記密碼的申請／完成／失敗）；下拉選項取自資料庫實際有的值 |
| 操作者 | `ActorAccount` / `ActorUserId` | 誰做的，顯示為「帳號（Id=N）」| 匿名或系統事件顯示「（系統／匿名）」，例如帳號不存在的登入失敗 |
| 目標 | `TargetType` / `TargetId` | 對什麼做的 | 顯示為 `型別#識別`，兩者皆空顯示破折號 |
| 摘要 | `Detail` | 補充說明，非敏感資訊 | 過長會截斷，完整內容在明細窗 |

**明細窗「稽核明細」**：發生時間（本地）、發生時間（UTC）、結果、動作代碼、操作者、目標類型、目標識別、摘要（空的顯示「（無）」）。

⚠️ 使用者異動的摘要只記帳號、角色與團隊；**勾選管理員、啟用狀態的變更不會寫進摘要**，團隊與分類的異動也不稽核。

> **清除與清空會記錄它自己。** 兩個動作成功後各寫一筆 `Audit.Purge` / `Audit.ClearAll`，
> 寫入排在刪除之後，所以那一筆會留在清空後的資料表裡 —— 否則「誰抹除了稽核紀錄」
> 會是整個系統唯一查不到的事。

> **時間偏掉一個時區是這一頁最容易出的錯。** `AuditLog` 是全專案唯一以 UTC 寫入的時間欄位；
> 例外紀錄與 Token 用量寫的都是本地時間。改這一頁時不要照抄那兩頁的時間處理。

---

## 五、統計與分析（僅管理員）

### 5.1 系統健康監控　`/system-health`

一頁看完系統是否正常：資料庫連得上嗎、磁碟還有空間嗎、AI 端點通不通。

| 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|
| 健康百分比 | `report.Score` | 得分 ÷ 總權重 × 100 | **總權重是 135 不是 100**（12 項）。新增一個檢查項會稀釋掉所有既有項目的佔比 |
| 燈號 | `report.Light` | 綠 ≥ 90、黃 ≥ 70、紅 < 70 | 每項正常給滿分、警示給一半、異常給 0 |
| 最後檢查時間 | `report.CheckedAt` | 這份報告產生的時間 | **開頁才檢查**，沒有背景排程，離開頁面就不會再更新 |
| 檢查項名稱／類別 | `item.Name` / `item.Category` | 例如「資料庫」`Database` | |
| 權重 | `item.Weight` | 這一項在總分裡的份量 | 權重高不代表比較嚴重，只代表比較影響分數 |
| 狀態 | `item.Status` | 正常／警示／異常 | |
| 佐證 | `item.Evidence` | 判斷依據的實際數值 | 排錯時真正有用的是這一欄 |
| 失敗訊息 | `item.FailureMessage` | 檢查失敗時的原因，以紅框顯示在該卡片內 | 只有出問題的項目才會出現 |
| 最後 100 筆日誌 | `report.LogTail` | 目前日誌檔的尾巴，並顯示日誌檔路徑 | 讀不到檔案時改顯示原因（`LogTail.Message`）|
| 寄信測試：目前的寄送方式 | `EmailSettings:Provider` | `None（未啟用）`、`Pickup` 或 `Smtp` | `None` 時下方輸入框與按鈕都停用 |
| 寄信測試：收件者 Email | `testEmailTo` | 測試信要寄給誰，預填目前登入者的 Email | 格式不對會被擋下、不會寄出 |
| 寄出測試信 | `EmailTestService.SendAsync` | **當場**寄一封測試信（不走背景佇列），結果以綠／紅訊息顯示 | `Pickup` 模式只寫成 `.eml` 檔、不會真的寄出。每次都寫一筆稽核 `Email.Test`（不記收件者）|

> ⚠️ **「LLM API」這一項每次開頁都會真的呼叫一次 API** —— 會計費，而且會以作業名稱
> 「系統健康檢測」出現在 Token 用量頁。常開這一頁會累積費用。
>
> 「寄信服務」這一項在 `Smtp` 模式下每次開頁會真的連一次 SMTP 主機並登入（**不寄信**，上限 5 秒）；
> `None`／`Pickup` 固定黃燈。

### 5.2 日誌檢視　`/logs`

查系統日誌，並可以請 AI 幫你整理成報告。

**這一頁可以做什麼**：依時間區間／最低等級／關鍵字查詢、匯出本次查詢結果（原始 nlog 格式，下載 `.log`）、
送 AI 分析、匯出 AI 分析的 PDF。結果清單最新在前，每頁 20 筆。

| 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|
| 起始／結束時間 | — | 查詢區間，預設最近 1 小時 | **最長 3 天**，超過會被夾回 |
| 最低等級 | `minimumLevel` | 只看這個等級以上（不限／TRACE／DEBUG／INFO／WARN／ERROR／FATAL）| 只能查**已經寫進檔案**的；等級門檻低於目前設定的內容根本沒被寫下來（見 5.3）|
| 筆數 | `takeCount` | 最多取幾筆，預設 100 | 1～10,000 |
| 關鍵字 | — | **比對整筆原始內容**，不只訊息 | |
| 時間 / 等級 / 記錄器 / 訊息 / TraceId | `Timestamp` / `Level` / `Logger` / `Message` / `TraceId` | 展開列可看原始內容 | TraceId 用來把同一次請求的多筆日誌串起來 |

**AI 分析**（按鈕提示「AI 分析（將本次查詢結果送給 AI 整理）」）

- 送出的是**畫面上已查出的結果**，不會重新查詢；最多送 `AiSettings:MaxEntries` 筆（預設 100），
  超過時保留最新的並提示。AI 未設定時按鈕停用，滑過去會顯示原因。
- 對話窗標題依狀態變化：AI 日誌分析（進行中）／（失敗）／（結果）。進行中顯示「已等待 N 秒」，
  關閉即取消，且不能點外面關閉。
- 結果頁可切換字級（一般／中／大）、「複製結果（Markdown 原文）」、「匯出 PDF 報告並下載」，
  並列出查詢區間、最低等級、關鍵字、分析範圍、使用模型、用量與耗時。
- 每次分析與匯出都會寫稽核（`LogViewer.AiAnalyze`／`LogViewer.AiAnalyzeExportPdf`），
  並以作業名稱「AI 日誌分析」出現在 Token 用量頁。

> AI 分析的結果會經過安全管線再渲染（移除圖片、限制連結協定）—— 日誌內容有一部分來自
> 使用者輸入，模型輸出屬於不可信內容。

### 5.3 日誌等級設定　`/log-level-setting`

調整「要記錄到多細」。沒有表格，只有兩張卡片與一組選項。

| 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|
| 目前生效等級 | `effectiveLevel` | 此等級以上才會寫入檔案 | |
| 系統預設等級 | `systemDefaultLevel` | `nlog.config` 裡設定的值 | |
| 選擇要輸出的最低等級 | `LevelOptions` | 套用後立即生效 | **只存在記憶體，重新啟動就回到系統預設**；也只影響應用程式自己的日誌 |

各選項的說明：Trace 最詳細，含框架層級細節；Debug 含查詢條件、換頁等排錯資訊；Info 含使用者操作軌跡，日常建議值；
Warn 僅警告以上；Error 僅錯誤以上；Fatal 僅嚴重錯誤。

- 「套用」在選擇與目前生效等級相同時停用；選 Trace 或 Debug 會先跳確認。
- 「還原為系統預設等級」回到 `nlog.config` 的值。
- 套用與還原都會寫稽核（`LogLevel.Apply`／`LogLevel.Restore`）。讀不到 NLog 設定時，整頁改顯示紅色警示。

### 5.4 系統例外紀錄　`/system-exceptions`

程式出錯時自動收集的清單。**不是逐筆事件**，而是「同一種錯誤聚合成一列並累計次數」。

**這一頁可以做什麼**：查詢、查看明細、刪除單列、匯出 CSV（最多 5,000 筆）、
清除 **90 天**未再發生的紀錄、清空全部。

**查詢條件**：最後發生（起）～（迄）、來源（不限／畫面／WebAPI／系統啟動／背景作業／未知）、
使用者帳號、關鍵字（比對類型、訊息、頁面、操作）。

刪除單列時會提醒：相同的例外若再次發生，會重新出現並從 1 次開始計算。

**清單欄位**

| 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|
| 最後發生 | `LastOccurredAt` | 這種錯誤最近一次發生的時間 | 預設排序欄 |
| 次數 | `OccurrenceCount` | **累計**發生幾次（100 次以上紅字、10 次以上橘字）| 與日誌檢視的筆數對不起來是正常的 —— 那邊是逐筆，這邊是聚合 |
| 類型 | `ShortExceptionType` | 例外型別（只顯示最後一節）| 滑過去可看完整型別名稱 |
| 訊息 | `Message` | 例外訊息 | |
| 頁面 | `Page` | 在哪一頁發生 | |
| 使用者 | `Account` | 誰遇到的 | |
| 操作 | — | 查看、刪除 | |

**明細窗「例外明細」**（以下欄位**只在明細窗**出現，清單上沒有）

| 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|
| 來源 | `Source` | 畫面、WebAPI、系統啟動、背景作業 | |
| 記錄器 | `LoggerName` | 寫出這筆日誌的類別 | |
| **操作（日誌訊息樣板）** | `Operation` | **是日誌的訊息樣板，不是使用者的操作** | 例如存的是 `Name={CategoryName}` 而不是 `Name=技術文件` —— 若存成算好的訊息，每個參數值都會變成一種新錯誤，列數會爆炸 |
| 首次發生／累計次數 | `FirstOccurredAt`／`OccurrenceCount` | 第一次出現的時間與總次數 | 判斷「這是新問題還是老問題」用 |
| 完整堆疊 | `StackTraceFile` | 存在資料庫外的檔案 | 開明細窗時才讀 |

### 5.5 資料庫用量　`/database-usage`

看資料庫佔了多少空間、哪張表最大。

**同一頁上有四種不同的「大小」，不要混用：**

| 畫面上的名稱 | 對應屬性 | 意思 | ⚠️ |
|---|---|---|---|
| 磁碟占用總計 | `TotalOnDiskBytes` | 主檔＋WAL＋SHM 的實際檔案大小 | 這才是「硬碟被吃掉多少」 |
| 主資料庫檔 | `MainDbBytes` | `.db` 檔本身 | |
| 預寫日誌 | `WalBytes` | SQLite 的 WAL 檔 | 會週期性縮回去，暫時偏大是正常的 |
| 共享記憶體索引 | `ShmBytes` | SQLite 的 SHM 檔 | |
| 已配置 | `AllocatedBytes` | 頁數 × 頁大小 | |
| 可回收 | `ReclaimableBytes` | 閒置頁，執行 VACUUM 可以還給作業系統 | 刪了很多資料但檔案沒變小，看這一欄 |
| 資料表 / 紀錄筆數 / 索引數 | `TableName` / `RowCount` / `IndexCount` | | |
| 資料量估算 | `EstimatedBytes` | **欄位位元組長度的加總** | ⚠️ **不是磁碟用量**：不含索引、不含碎片、不含閒置頁。加總起來也不會等於上面任何一個數字 |

### 5.6 Token 用量　`/token-usage`

每一次呼叫 LLM 的 token 數與花費。共 **7 個頁籤**，依序為：趨勢圖（預設）、每日趨勢、依使用者、依作業、
依模型、依型別、明細（除趨勢圖外，標籤上都帶筆數，例如「明細（N）」）。

**這一頁可以做什麼**：

- 查詢條件：帳號（部分比對）、起始日、結束日、作業、型別、模型。
- 工具列：查詢、重新整理、匯出目前查詢結果（CSV）、下載全部頁籤的 PDF 報表、
  只匯出目前頁籤的 PDF（0.9.51 起，按鈕文字帶目前頁籤名稱）、清空全部紀錄。0 筆時 CSV 與兩個 PDF 按鈕停用。
- 清除舊資料：選「清除此日之前」的日期，再按紅色「批次清除」。
- 明細頁籤可逐筆「詳細」「刪除」。

PDF 檔名為 `<專案>.Web-llm-usage-{頁籤-}yyyyMMdd-HHmmss.pdf`（頁籤代號 trend／daily／account／operation／model／callkind／detail）。

**合計卡（0.9.53 起，此前是單行橫幅）**：標題列「篩選範圍合計」下方四張卡。

| 畫面上的名稱 | 意思與用途 | ⚠️ |
|---|---|---|
| 輸入（送出／prompt）| 這批呼叫送出的 token，卡內副行是「其中快取」 | 快取是**輸入的折扣子集**，不另計入合計 |
| 輸出（接收／completion）| 模型回傳的 token，卡內副行是「其中推理」 | 推理**計入輸出**，不另計入合計 |
| 合計 | 輸入 ＋ 輸出 | 子集不重複計入 —— 這也是**只有四張卡、快取與推理不各自成卡**的原因：六張對等的卡會讓人把它們相加 |
| 花費 | NT$（副行 US$）| 未定價不計入金額，另以橘字標出「⚠ 其中 N 筆未定價未計入」；沒有未定價時副行說明「美金與台幣各自逐列加總」|

標題列完整文字為「篩選範圍合計　—　套用條件：…」。
⚠️ 這排的「套用條件」**含日期**，下方的近 N 天卡**不含**（那排會換掉日期）。
⚠️ 這排**不可點**；可點的是下面那排。

**「最近 N 天」摘要卡（0.9.50 起）**：合計卡下方三張卡，標題列「最近用量與費用（點卡片可套用該區間）」，
點下去就把該區間套進起訖日重查；目前套用中的那張會標亮，有未定價時同樣標出。

| 畫面上的名稱 | 意思與用途 | ⚠️ |
|---|---|---|
| 近 1 天（今天）／近 7 天／近 30 天 | 該區間的 NT$、US$、呼叫次數、合計 token | 「最近 N 天」＝**含今天往前數 N 日**，近 7 天是 `today-6` 到 `today`，不是 8 天 |
| （卡片上方）套用條件：… | 目前除了日期以外還套了哪些篩選 | 卡片**會跟著帳號／作業／型別／模型篩選走**，只有日期換成自己的窗。看到小數字先確認這一行 |

**趨勢圖頁籤（0.9.55 起，排第一個、預設頁籤）**：上下兩張折線圖，共用日期軸。

| 畫面上的名稱 | 意思與用途 | ⚠️ |
|---|---|---|
| 每點 = 1 天／每點 = 1 週（週一起算）| 目前一個點代表多長的時間 | 區間不超過 92 天按日，93～180 天自動改按週；頭尾不滿一週的點，滑鼠提示會寫出實際起訖日 |
| 費用（NT$）| 上圖，每點的 `CostTwd` 合計 | 未定價的呼叫不計入，那些點會低於實際用量（圖下方會提示）|
| 合計 token | 下圖，每點的 `TotalCount` 合計 | 與上圖**各自一個 Y 軸**，兩條線的高低不可互相比較 |
| （滑鼠移到點上）| 期間、NT$（US$）、合計 token、呼叫次數、未定價筆數 | — |

圖下方說明「每點 = 1 天／1 週（週一起算），共 N 點；滑鼠移到點上可看數字。」；區間內沒有資料時顯示「這段期間沒有任何呼叫紀錄。」
區間、篩選與 180 天上限都與「每日趨勢」頁籤相同（兩者用同一份資料）。

**每日趨勢頁籤（0.9.50 起）**：一天一列，兩根純 CSS 長條。

| 畫面上的名稱 | 意思與用途 | ⚠️ |
|---|---|---|
| 費用（NT$）長條 | 當日 `CostTwd` | 與 token 長條**各自歸一化，兩根不可互相比較** —— 單位不同（錢 vs token）|
| 合計 token 長條 | 當日 `TotalCount` | 同上 |
| （沒有資料的日期）| 補零成空白列 | 不補零的話，長條圖會把「那天沒花錢」畫成「那天不存在」 |

每列另顯示日期（`MM/dd (星期)`）、NT$ 金額、合計 token 與「N 次」。
趨勢區間跟隨篩選；起訖日都沒填時用近 30 天，只填一端時另一端取今天或最早有資料的日期，且**上限 180 天**（超過只顯示最後 180 天並標註）。

**依使用者／依作業／依模型／依型別頁籤**：同一組欄位 —— [分組鍵]、輸入、輸出、快取、推理、合計、
費用（NT$，滑過顯示 US$；有未定價時加註「（N 未計）」）、次數。

**加總語意（本頁最容易誤解的地方）** ⚠️

```
輸入（prompt）
 ├─ 其中快取命中   ← 是輸入的「折扣子集」，不另外加
 ├─ 其中圖片輸入   ← 同樣是子集
輸出（completion）
 ├─ 其中推理       ← 計入輸出，不另外加
 └─ 其中圖片輸出   ← 同樣是子集
合計 ＝ 輸入 ＋ 輸出        ← 子集一律不重複計入
```

畫面上寫「**其中**快取／**其中**推理」就是為了防止你把它們再加一次。

**明細頁籤**

| 畫面上的名稱 | 對應屬性 | 意思與用途 | ⚠️ |
|---|---|---|---|
| 時間 | `OccurredAt` | 呼叫發生時間 | |
| 使用者 | `Account` | 誰觸發的 | 空的顯示「（系統自動）」—— 例如健康監控的探測 |
| 作業 | `Operation` | **應用層的業務名稱**，例如「系統健康檢測」 | 不是 API 概念 |
| 型別 | `CallKind` | 呼叫種類 | |
| 模型 | `Model` | | 清單只顯示模型；供應商在明細窗與 CSV |
| 輸入 / 輸出 / 推理 / 快取 / 合計 | `InputCount` / `OutputCount` / `ReasoningCount` / `CachedInputCount` / `TotalCount` | 見上面的加總語意 | 失敗的呼叫合計欄顯示紅色「失敗」；依時長計費的顯示「時長 N 秒」 |
| 費用 | `CostTwd`（提示顯示 US$、匯率、費率鍵、長脈絡費率）| 這次呼叫花了多少，可排序 | **是呼叫當下的快照**。之後改費率表**不會**回頭修正 |
| （合計卡【花費】）⚠ N 筆未定價未計入 | `IsUnpriced` | 找不到費率的筆數 | **「未定價」不等於 0** —— 記成 0 會讓漏帳看起來像免費 |
| 當時匯率 | `CostExchangeRate` | 換算 TWD 用的匯率快照 | 台幣與美金總額各自逐列加總，**不能用單一匯率互換** |
| 計價依據 | `CostPriceKey` | 實際套用到哪一組費率 | 費率是**前綴比對**的：`gpt-4o-2024-08-06` 會套用 `gpt-4o` 的費率。對不上兄弟模型時看這一欄 |
| 音訊時長／字元數 | `DurationSeconds` / `CharacterCount` | 另外兩種計費單位 | 同一張表裡混著三種計費單位，「合計」欄的意義會逐列改變 |
| 操作 | — | 詳細、刪除 | |

當時匯率、計價依據、音訊時長／字元數**不是清單欄位**，出現在費用的滑鼠提示與明細窗。

**明細窗「Token 用量明細」**另外列出：供應商、耗時（ms）、結果／失敗原因、紀錄 Id、圖片輸入、圖片輸入快取、
圖片輸出、長脈絡費率、套用的費率明細、原始明細（供應商回傳）。

**CSV 欄位（24 欄）**：時間、使用者、作業、型別、供應商、模型、輸入、輸出、推理、快取、圖片輸入、圖片快取、
圖片輸出、合計、費用USD、費用TWD、匯率、計價依據、長脈絡、字元數、音訊時長秒、耗時ms、結果、失敗原因。

---

## 六、沒有網址、但也是畫面

### 6.1 右上角使用者選單

顯示目前登入者的名稱，管理員會多一個「管理員」標記。展開後有：變更密碼、設定 API 密碼、關於、登出。

### 6.2 「關於」對話窗

顯示六項：系統名稱、系統描述、系統版本、執行環境、啟動時間、已運作時間。
回報問題時附上這六項最有幫助。（0.4.45 起不再顯示 .NET 版本。）

### 6.3 斷線重連提示

跟伺服器的連線斷掉時會蓋住整個畫面。這是 Blazor Server 的特性 ——
畫面狀態存在伺服器端，連線斷了就無法操作，**不是離線模式**。提示文字是英文（Rejoining the server… ／ Retry ／ Resume）。

### 6.4 側邊選單

預設是**收合**狀態，按切換鈕（「展開側邊欄」／「收起側邊欄」）展開；收合時滑過群組會彈出子選單。
項目依權限過濾，一個都沒有時顯示「尚無可用選單」。點「登出」一樣會先跳確認。

---

## 七、錯誤頁

| 畫面 | 網址 | 說明 |
|------|------|------|
| 錯誤頁 | `/Error` | 未預期的例外（只在非開發環境啟用）。同一個錯誤會被自動收進 5.4 的系統例外紀錄 |
| 找不到頁面 | `/not-found` | 網址不存在時顯示（404 由伺服器**在原網址重新執行**這一頁，網址列不會變）。不需登入，內容為英文「Not Found」|

---

> **沒有權限時不會導到這兩頁。** 檢視會留在原地、顯示一塊紅色提示（例如「您沒有權限檢視系統健康監控。」），
> 側邊選單則根本不會出現該項目 —— 所以「選單裡沒看到」與「點進去被擋」是兩種不同的狀況。

---

## 延伸閱讀

- [各能力 PRD](../prd/README.md) —— 每一頁「怎麼運作」
- [開發慣例與限制速查](../architecture/開發慣例與限制速查.md) —— 改這些畫面前必讀
- [介面視覺設計規範](../architecture/介面視覺設計規範.md)、[對話窗 UI 設計規範](../architecture/對話窗%20UI%20設計規範.md)
- [紀錄分類與團隊權控 PRD](../prd/紀錄分類與團隊權控-prd.md) —— §0.1／§0.2 的完整設計脈絡

> 返回 [文件總索引](../README.md)
