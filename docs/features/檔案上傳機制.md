# 檔案上傳機制

- 文件版本：1.2
- 文件狀態：已實作
- 現行系統版本：0.4.35
- 首次實作版本：0.1.61
- 最後核對日期：2026/08/26

## 文件目的

說明專案紀錄的附件如何上傳、儲存、刪除，以及實體檔案的目錄組織策略。

> 0.4.24 起「工作項目」與「會議記錄」已移除，其附件機制（`MyTasFile` / `MeetingFile`、`TaskFilePath` / `MeetingFilePath`）一併下架。新增其他領域模組時可比照本文件的 `Project` 樣板複刻。

---

## 1. 附件對照

| 主表 | 附件 Entity | 設定檔欄位（`SystemSettings.ExternalFileSystem`） |
|------|--------------|----------------------------------------------------|
| `Project` | `ProjectFile` | `ProjectFilePath` |

預設值（`appsettings.json`）：

```json
"ProjectFilePath":  "C:\\temp\\MyProject\\ProjectFile"
```

啟動時 `Program.cs:132-141` 會呼叫 `EnsureDirectoryExists(...)` 自動建立缺少的目錄。

---

## 2. 目錄組織策略

每筆紀錄的附件依「主表的建立時間」決定目錄分層：

```
{各模組根目錄}/
└── {年}/                ← 主表 CreatedAt.Year
    └── {月}/             ← 主表 CreatedAt.Month（補零兩碼）
        ├── 檔案 A
        ├── 檔案 B
        └── ...
```

例：2026 年 5 月新增的專案，其檔案會落在 `C:\temp\MyProject\ProjectFile\2026\05\` 下。

---

## 3. 上傳行為

- 一筆紀錄可上傳多個檔案。
- 單檔最大 1GB（前端與後端皆需驗證）。
- **副檔名白名單**（0.4.35 起，`MyProject.Business/Helpers/UploadFileTypePolicy.cs`）：
  預設涵蓋文件、試算表、簡報、圖片與封存格式，刻意**不含**
  `.html/.htm/.svg/.xhtml/.js/.exe/.bat/.cmd/.ps1/.sh`。
  各案可於 `SystemSettings:Upload:AllowedExtensions` 覆寫；留空採用內建預設。
- **ContentType 一律依副檔名決定**，不採用呼叫端提供的值 ——
  攻擊者可以上傳 `.txt` 卻宣稱是 `text/html`；配合 `nosniff` 標頭，避免同源 stored XSS。
- 在「新增模式」下，使用者上傳的檔案會先暫留於 UI；按下「儲存」後，Service 才將實體檔案落地到對應年月目錄，並建立資料庫紀錄。
- 在「修改模式」下：
  - 既有檔案會列在清單中，可手動移除（標記為待刪除）。
  - 可繼續加入新檔案，落地行為同新增模式。
  - 按下「儲存」後：被標記為待刪除的紀錄連同實體檔案一併移除；新檔案則寫入目錄並登入資料庫。

---

## 4. 刪除行為

當使用者刪除主表紀錄（例如刪除一筆專案）：

1. EF Core 因 `DeleteBehavior.Cascade`（見 `BackendDBContext.OnModelCreating`）會自動刪除附件資料表的 row。
2. **實體檔案**仍須由 Service 層在刪除前蒐集 `StoredPath`、刪除主表後再呼叫 `File.Delete(...)`。
3. 若實體檔案刪除失敗（例如已被外部移動），Service 應寫入 `Warning` 等級日誌，但不阻斷主表刪除流程。

---

## 5. 對應 DTO 與設定強型別

`SystemSettings.ExternalFileSystem` 透過 `IOptions<SystemSettings>` 注入到 Service：

```csharp
public class FooService(IOptions<SystemSettings> options, ILogger<FooService> logger)
{
    private readonly ExternalFileSystem fileSystem = options.Value.ExternalFileSystem;
    // ...
}
```

於 Service 內統一使用 `fileSystem.ProjectFilePath` 等屬性，**禁止**直接從 `IConfiguration` 讀字串。

---

## 6. `/UploadFiles` 靜態資源對應

`Program.cs:319-325`：

實作於 `MyProject.Web/Extensions/ApplicationBuilderExtensions.cs` 的
`UseConfiguredDownloadStaticFiles`，註冊於 `UseAuthentication()` / `UseAuthorization()` **之後**，
並以 `UseWhen` 分支加上「必須已驗證身分」的閘門（未登入回 401）。

> 沿革：0.4.35 之前它掛在驗證中介軟體**之前**，而 `UseStaticFiles` 本身不看授權，
> 等於整個 `DownloadPath` 目錄匿名可讀。

- `DownloadPath` 是「登入者皆可下載」目錄；任何放在這裡的檔案都會被 `/UploadFiles/...` 服務給**所有已登入使用者**（不做團隊或權限過濾）。
- 附件目錄（`ProjectFilePath`）**不**直接對應 `/UploadFiles`；下載這些附件需透過 Controller 端點，由 Service 驗證權限後再回傳檔案串流。

---

## 7. 實作檢查清單

新增「附件功能」時請確認：

- [ ] Entity 已新增 `XxxFile` 子表，並在 `BackendDBContext.OnModelCreating` 設定 `Cascade`。
- [ ] `appsettings.json` 已新增對應根目錄欄位，並在 `SystemSettings.ExternalFileSystem` 模型中對映。
- [ ] `Program.cs:132-141` 已呼叫 `EnsureDirectoryExists` 包含新目錄。
- [ ] Service 對外提供 `Add` / `Update` / `Delete` 時皆同步處理實體檔案。
- [ ] 對外下載端點需做角色檢查與檔名清洗，避免路徑跳脫攻擊。

---

## 8. 延伸閱讀

- [資料模型與資料庫](../architecture/資料模型與資料庫.md) — Cascade 行為、附件 Entity 欄位。
- [Web API 設計慣例](../architecture/Web%20API%20設計慣例.md) — 上傳 / 下載端點命名建議。
- [日誌與設定檔說明](../operations/日誌與設定檔說明.md) — `SystemSettings.ExternalFileSystem` 各欄位完整對照。
