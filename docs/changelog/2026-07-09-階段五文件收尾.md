# 階段五：文件收尾（權限重構 RBAC 落地後的文件對齊，0.4.22，無版本變更）

## 目的

權限重構路線圖的最後一步。階段 0～4 的 RBAC（四表、多角色、動作級授權、宣告式選單、UI/API 單一權威來源、PBKDF2、帳號鎖定、稽核表、TOTP 骨架）已全數落地，但多份文件仍以**重構前現況**書寫，其中兩處 AI-facing 不變量正在誤導。本次把文件對齊到已出貨的現實。**純文件、無程式碼/schema 變更、不改版本號。**

## 變更範圍

- **`CLAUDE.md` / `AGENTS.md`（不變量速查）**
  - 「新增頁面權限三處同步（位置索引對應）」→ 改為宣告式：`Menu.json`（唯一 `id`）＋ `SidebarMenuService.MenuPermissionMap`（`id→權限鍵`）＋ `MagicObjectHelper` 常數，重排不錯位。
  - API 不變量補動作級授權：`[HasPermission("resource:action")]` + `IPermissionChecker`，無權限回 403、UI/API 共用單一權威來源。
- **`docs/architecture/開發慣例與限制速查.md`**：§4 補動作級授權（403）；§5 補動作粒度、多角色聯集、UI/API 共用 `IPermissionChecker`；§7 seed 密碼由 SHA 更正為 **PBKDF2**（`SecurePasswordHasher`）+ 登入自動遷移、並註明 `RolePermissionMap` 由 `RbacBackfillService` 回填。
- **`docs/architecture/資料模型與資料庫.md`**：§3.1 過時警語「階段二僅建立回填、執行期仍舊模型」→ 更正為「執行期授權已以關聯表為權威、API 已強制動作級授權」；§4.2 seed 帳號/密碼更正（SupportAccount、PBKDF2）、補 `RolePermissionMap` 回填說明。
- **`docs/architecture/Web API 設計慣例.md`**：新增「功能級／動作級授權」節（`HasPermissionAttribute` + `IPermissionChecker`、動作對應、兩型權限鍵、403、管理員短路）；Controller 規範補 `[HasPermission]` 要求；403 狀態碼補來源。
- **`docs/operations/正式部署與安全檢查清單.md`**：新增查核項——以無權限使用者實測 create/edit/delete 回 403（非 200）。
- **`docs/security/權限授權現況評估與改善路線.md`**：§一/§二/§三加「現況更新」callout，逐項對應已解決（#1~#8）與暫緩（#9）；路線圖階段 5 標記 ✅ 完成。

## 已查證的事實（撰寫依據）

- 選單為 `SidebarMenuService.MenuPermissionMap`（`Id→權限鍵`）宣告式，`ApplyPermissionStructure` 以 `TryGetValue(item.Id, …)`，非陣列索引。
- 種子密碼：`Program.cs` `support` 帳號用 `SecurePasswordHasher.HashPassword`（PBKDF2），既有帳號驗證失敗自動升級；預設角色 `TabViewJson` 由啟動 `RbacBackfillService` 回填至 `RolePermissionMap`。

## 注意事項（未納入本次、另行提報）

- `docs/architecture/資料模型與資料庫.md` §4.1 仍寫「目前僅一份初始 Migration」，實際已有 8 份（`init`…`AddRbacTables`）。此為與 RBAC 授權無直接關係的獨立過時項，未在本階段範圍內修正，建議另案處理。
- `docs/security/認證授權與權限機制.md` 已於 0.4.22 更新，本階段未改。

## 驗證

- `scripts/Test-DocsEncoding.ps1` exit 0（所有 `docs/*.md` 含 BOM、無亂碼）。
- grep 確認 `CLAUDE.md`/`AGENTS.md` 不再殘留「位置索引/三處同步」舊描述。
