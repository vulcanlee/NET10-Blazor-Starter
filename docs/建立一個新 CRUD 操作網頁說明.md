# 建立一個新 CRUD 操作網頁說明

## 建立資料庫存取類別
* 在 [MyProject.AccessDatas] 專案下的 [Models] 目錄下建立一個新的類別
* 使用 EF Core Migration 命令，進行更新資料庫結構

## 建立一個有頁面路由的元件
* 在 [Components] > [Pages] 目錄下建立一個新的元件
* 在此元件中，加入可以路由該該頁面的網址
* 在此頁面中，加入參考的 View 元件
